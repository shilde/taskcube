services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: taskcube
      POSTGRES_USER: taskcube
      POSTGRES_PASSWORD: taskcube
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U taskcube -d taskcube"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
